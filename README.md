# shared_memory_entity
User space shared memory entities: memory area manager, interprocess exchange channels, adaptation for standard template
library(STL) containers.

The documentation is in progress...
